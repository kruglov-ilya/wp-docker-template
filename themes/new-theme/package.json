{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsc && vite build && vite",
    "build": "tsc && vite build"
  },
  "devDependencies": {
    "rollup-plugin-external-globals": "^0.9.1",
    "typescript": "^5.2.2",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "wordpress-vite-plugin": "^0.1.0"
  }
}
